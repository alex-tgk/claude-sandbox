/**
 * Modular UI System
 *
 * @remarks
 * A production-ready, accessible UI component library for AI-driven web applications.
 *
 * Features:
 * - React 18+ with TypeScript (strict mode)
 * - Tailwind CSS v4 for styling
 * - WCAG 2.2 AA accessible
 * - SSR-friendly
 * - Tree-shakable
 * - Fully typed
 *
 * @packageDocumentation
 * @since 0.1.0
 */

// Components
export * from './components';

// Hooks
export * from './hooks';

// Utilities
export * from './utils';

// Types can be imported separately if needed
export type { ClassValue } from 'clsx';
